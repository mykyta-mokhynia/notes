- **2026-04-23**
  - **Type of change:** Added resilient local draft recovery and unsaved-change safeguards for visual database schema editor.
  - **Files affected:** `frontend/src/app/workspace/note-editor/database-schema-editor/database-schema-editor.ts`, `frontend/src/app/workspace/note-editor/unified-note-editor.ts`, `frontend/src/styles.scss`, `AI_LOGS.md`.
  - **Reason:** Preserve table/schema edits across reloads or offline interruptions by auto-saving editor drafts to local storage, restoring them on reopen, showing explicit `You have unsaved changes` signal, confirming discard before close without apply, and surfacing a small outside-the-editor badge on schema cards when a local draft exists; then refining the badge layout to the top-right inline with the schema title and changing label copy to English (`Local draft`), fixing badge visibility so `[hidden]` state is respected (it had been overridden by explicit `display` style), wiring note header status/warning style to reflect schema local drafts too, adding direct schema-editor unsaved-state propagation to the parent (`unsavedChange` output) so top status updates immediately on schema moves/edits even before local-storage/event synchronization, removing the extra in-editor `Recovered local draft (not saved)` text by request, and fixing note-level badge detection for tokenized note URLs (`/home/notes/<slug--id>`) by parsing token to raw note id before draft-key lookup.
  - **Structural impact:** No structural changes; behavior and UI updates inside existing note editor and schema editor components.
